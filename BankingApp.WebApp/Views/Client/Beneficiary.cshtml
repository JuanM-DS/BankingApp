@using BankingApp.Core.Application.ViewModels.Beneficiary
@model List<BeneficiaryViewModel>
@{
    ViewData["Title"] = "Beneficiarios";
}

<div class="container-fluid">

    <div class="row mb-3">
        <div class="col-3 offset-9">
            <button type="button" class="btn btn-outline-success" data-bs-toggle="modal" data-bs-target="#addBeneficiaryModal">Añadir beneficiario</button>

        </div>
    </div>

    <div class="row">

        @if (Model == null || Model.Count == 0)
        {
            <h2>No hay beneficiarios</h2>
        }
        else
        {
            @foreach (var item in Model)
            {
                <div class="col-4">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>Nombre:</th>
                                        <td>Armando</td>
                                    </tr>
                                    <tr>
                                        <th>Apellido:</th>
                                        <td>Hernandez</td>
                                    </tr>
                                    <tr>
                                        <th>Numero de cuenta:</th>
                                        <td>098568432</td>
                                    </tr>
                                </tbody>
                            </table>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a asp-controller="" asp-action="" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Borrar</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
    </div>

</div>


<div class="modal fade" id="addBeneficiaryModal" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content px-4">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="modalLabel">Agregar Beneficiario</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form asp-controller="Beneficiary" asp-action="Add" method="post">
                <input required type="text" class="form-control" name="accountNumber" />


                <div class="modal-footer">
                    <button type="button" class="btn btn-warning" data-bs-dismiss="modal">Cancelar</button>
                    <input type="submit" class="btn btn-success" value="Agregar Beneficiario" />
                </div>
            </form>
        </div>
    </div>
</div>